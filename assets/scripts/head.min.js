function correctFavicon() {
    document.querySelectorAll('link[rel=icon],link[rel=apple-touch-icon],link[rel=mask-icon],meta[name=msapplication-TileImage]').forEach(function (node) {
        node.setAttribute(node.hasAttribute('content') ? 'content' : 'href', '/assets/images/logos/wiki-logo.svg');
    });
}
function generateCanonicalUrl(options = {}) {
    var { removeExtension: options = !0 } = options,
        currentURL = new URL(window.location.href);
    let canonicalURL = currentURL.origin + currentURL.pathname;
    return (canonicalURL = options ? canonicalURL.replace(/\.[^/.]+$/, '') : canonicalURL);
}
function addCanonicalLink(canonicalURL, replace = !0) {
    var head = document.head,
        replace =
            (replace &&
                (replace = head.querySelector('link[rel="canonical"]')) &&
                (console.warn('Existing canonical link found, replacing it:', replace.href), replace.remove()),
            document.createElement('link'));
    (replace.rel = 'canonical'), (replace.href = canonicalURL), head.appendChild(replace), console.info('Canonical link added:', replace.href);
}
function correctOgUrl(url) {
    var meta = document.querySelector('meta[property="og:url"]');
    meta && (meta.setAttribute('content', url), console.info('og:url corrected:', url));
}
function main() {
    correctFavicon();
    var canonicalURL = generateCanonicalUrl();
    addCanonicalLink(canonicalURL, !0), correctOgUrl(canonicalURL);
}
